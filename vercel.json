{
  "version": 2,
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "regions": ["iad1"],
  "installCommand": "npm install --platform=linux --arch=x64",
  "buildCommand": "npm run build"
}
